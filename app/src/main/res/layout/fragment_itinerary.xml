<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/itinerary_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp">

    <!-- Top filters row -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/top_filters"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Spinner
            android:id="@+id/spinner_filter"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/custom_input"
            android:paddingHorizontal="12dp"
            android:spinnerMode="dropdown"
            android:entries="@array/itinerary_filter_options"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <EditText
            android:id="@+id/et_search_trips"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/custom_input"
            android:hint="Search for trip"
            android:drawableStart="@drawable/icon_search4"
            android:drawablePadding="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="40dp"
            android:singleLine="true"
            android:imeOptions="actionSearch"
            android:inputType="text"
            app:layout_constraintStart_toEndOf="@id/spinner_filter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageButton
            android:id="@+id/btn_calendar"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="16dp"
            android:background="@android:color/transparent"
            android:padding="0dp"
            android:src="@drawable/icon_calendar"
            app:layout_constraintEnd_toEndOf="@id/et_search_trips"
            app:layout_constraintTop_toTopOf="@id/et_search_trips"
            app:layout_constraintBottom_toBottomOf="@id/et_search_trips" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Recent Trip header -->
    <TextView
        android:id="@+id/tv_recent_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/montserrat_bold"
        android:text="Recent Trip"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/top_filters"
        app:layout_constraintStart_toStartOf="@id/top_filters" />



    <ImageButton
        android:id="@+id/btn_toggle_recent"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_arrow_down"
        app:layout_constraintBottom_toBottomOf="@id/tv_recent_label"
        app:layout_constraintEnd_toEndOf="@id/top_filters" />

    <!-- Recent Trip card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_recent_trip"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="14dp"
        app:cardUseCompatPadding="true"
        app:cardElevation="2dp"
        app:layout_constraintTop_toBottomOf="@id/tv_recent_label"
        app:layout_constraintStart_toStartOf="@id/top_filters"
        app:layout_constraintEnd_toEndOf="@id/top_filters">

        <include layout="@layout/view_trip_card" />
    </androidx.cardview.widget.CardView>

    <!-- Your Trips header -->
    <TextView
        android:id="@+id/tv_your_trips_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/montserrat_bold"
        android:text="Your Trips"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/card_recent_trip"
        app:layout_constraintStart_toStartOf="@id/top_filters" />

    <!-- Your Trips list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_your_trips"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="8dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/tv_your_trips_label"
        app:layout_constraintStart_toStartOf="@id/top_filters"
        app:layout_constraintEnd_toEndOf="@id/top_filters"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
